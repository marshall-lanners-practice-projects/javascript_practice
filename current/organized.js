/*
	functions regex and string

	exec, test, replace

	let str = 'a long string here'
	let regex = /[^aeiouy]/g

	let test = str.match(regex).join('')

	console.log(test)
	//this will remove all vowels from the string

	let str = 'a long string here'
	let regex = /[aeiouy]/g

	let test = str.replace(regex, '8')

	console.log(test)
	//this will replace all the vowels with the number 8

*/